#include "stdafx.h"
/*
#include <iostream>
#include <cstdio>  
#include <string>  
#include <cstring>
#include <vector>
#include <stdio.h>
#include <math.h>
#include <algorithm>
#include <iomanip>

using namespace std;

// 宏声明

// 全局变量声明
string day[7]={"MON", "TUE", "WED", "THU", "FRI", "SAT", "SUN"};

// 函数定义


int main()
{
	string a[4];
	for(int k=0; k<4; k++)
		cin >> a[k];
	for(int i=0,int cnt=0;i<a[0].size()||i<a[1].size(); i++)
	{
		if(a[0][i]==a[1][i])
		{
			cnt++;
			if(cnt==1)
			{
				cout << day[a[0][i]-'A'+1] << " ";
			}
			else if(cnt==2)
			{
				cout << (a[0][i]-'0') << ":";
			}
		}
			
	}
	for(int j=0,int cnt1=1;j<a[2].size()||j<a[3].size(); j++)
	{
		if(a[2][j]==a[3][j])
			cout << j;
	}




	return 0;
}
*/